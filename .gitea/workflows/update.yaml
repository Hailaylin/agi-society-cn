name: ci
on:
  push:
    branches:
      - master
      - main
jobs:
  deploy:
    runs-on: linux-amd64
    steps:
      - run: |
          ls -al
      - run: |
          cd ..
      - run: |
          ls -al
      - run: |
          cd ..
      - run: mkdocs gh-deploy