name: ci
on:
  push:
    branches:
      - master
      - main
jobs:
  deploy:
    runs-on: linux-amd64
    steps:
      - run: |
          ls -al
      - run: |
          cd ${{ vars.WEB_WORK_DIR }}
      - run: |
          ls -al
      - run: mkdocs gh-deploy